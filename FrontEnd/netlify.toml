# Proxy para redirigir peticiones /api/* al backend Express
[[redirects]]
  from = "/api/*"
  to = "http://localhost:3000/api/:splat"
  status = 200
[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/api/*"
  to = "http://localhost:3000/api/:splat"
  status = 200
